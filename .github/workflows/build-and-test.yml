name: Build and test

on:
  pull_request:
    branches: [ master, main, gtnh ]
  push:
    branches: [ master, main, gtnh ]

jobs:
  build-and-test:
    uses: GTNewHorizons/GTNH-Actions-Workflows/.github/workflows/build-and-test.yml@master
    secrets: inherit
